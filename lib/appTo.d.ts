/**
 * 调用app指令
 * @param cmd 指令
 * @param param 指令参数
 * @param appVersion app版本
 * @param iosFixVersion ios下指令调用方法修复
 */
export default function appTo(cmd: string, param: string, appVersion?: string, iosFixVersion?: string): void;
